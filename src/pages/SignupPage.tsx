import {useState,type FormEvent} from 'react';import {Link,useNavigate} from 'react-router-dom';import {useAuth} from '../contexts/AuthContext';
export function SignupPage(){const {signUp,configured}=useAuth();const nav=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [error,setError]=useState('');const [notice,setNotice]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setError('');if(password!==confirm){setError('Passwords do not match.');return;}setBusy(true);const message=await signUp(email,password);setBusy(false);if(message)setError(message);else{setNotice('Account created. If email confirmation is enabled, check your inbox before signing in.');setTimeout(()=>nav('/login'),1200)}};return <div className="screen auth-page page-enter"><div className="auth-card"><span className="eyebrow">CREATE ARCHIVE ACCOUNT</span><h2>Save your shinobi.</h2><p>Your quiz results and characters can follow you across devices.</p>{!configured&&<div className="generator-warning">Supabase is not configured yet.</div>}<form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={6}/></label><label>Confirm password<input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} required minLength={6}/></label>{error&&<div className="generator-error">{error}</div>}{notice&&<div className="generator-warning">{notice}</div>}<button className="btn primary full" disabled={busy||!configured}>{busy?'Creating…':'Create account'}</button></form><p className="auth-switch">Already have an account? <Link to="/login">Sign in</Link></p></div></div>}
