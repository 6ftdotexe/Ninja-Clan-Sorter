import {useState} from 'react';
import {useNavigate} from 'react-router-dom';
import {useArchive} from '../store/useArchive';
export function HomePage(){const navigate=useNavigate();const {name,setName}=useArchive();const [draft,setDraft]=useState(name);return <div className="hero page-enter"><span className="eyebrow">SHINOBI IDENTITY ARCHIVE · V9</span><h1>Discover, build, and share your shinobi identity.</h1><p className="lede">Thirteen connected identity trials, cloud accounts, saved characters, progression, AI character generation, and public profiles inside a growing Shinobi World.</p><div className="namebox"><input value={draft} onChange={e=>setDraft(e.target.value)} placeholder="Shinobi name (optional)"/><button className="btn primary" onClick={()=>{setName(draft.trim());navigate('/archive')}}>Enter archive</button><button className="btn secondary" onClick={()=>navigate('/discover')}>Explore World</button></div><div className="meta"><div><strong>13</strong><span>IDENTITY TRIALS</span></div><div><strong>16</strong><span>CLANS</span></div><div><strong>∞</strong><span>PROFILE COMBOS</span></div><div><strong>AI</strong><span>CHARACTER ART</span></div><div><strong>V9</strong><span>PUBLIC ARCHIVE</span></div></div></div>}
