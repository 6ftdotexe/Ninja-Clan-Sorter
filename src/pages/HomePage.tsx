import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {useArchive} from '../store/useArchive';
export function HomePage(){const navigate=useNavigate();const {name,setName}=useArchive();const [draft,setDraft]=useState(name);return <div className="hero page-enter"><span className="eyebrow">SHINOBI IDENTITY ARCHIVE · V7</span><h1>Build a shinobi profile that actually feels earned.</h1><p className="lede">Six connected personality trials, a reusable scoring engine, randomized clan scenarios, elemental affinity, summon compatibility, and a final dossier.</p><div className="namebox"><input value={draft} onChange={e=>setDraft(e.target.value)} placeholder="Shinobi name (optional)"/><button className="btn primary" onClick={()=>{setName(draft.trim());navigate('/archive')}}>Enter archive</button></div><div className="meta"><div><strong>6</strong><span>CONNECTED TRIALS</span></div><div><strong>16</strong><span>CLANS</span></div><div><strong>40</strong><span>CLAN SCENARIOS</span></div><div><strong>10–12</strong><span>OTHER TEST QUESTIONS</span></div><div><strong>V7</strong><span>ROUTED REACT</span></div></div></div>}
