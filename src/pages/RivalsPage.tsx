import {useEffect,useMemo,useState} from 'react';
import {useNavigate} from 'react-router-dom';
import {useAuth} from '../contexts/AuthContext';
import {useArchive} from '../store/useArchive';
import {listCharacters,type ShinobiCharacter} from '../services/characterService';
import {listPublicCharacters} from '../services/publicProfileService';
import {addRival,listRivals,removeRival} from '../services/socialService';
import type {ShinobiRival} from '../types/social';

export function RivalsPage(){const {user}=useAuth();const nav=useNavigate();const {activeCharacterId}=useArchive();const [own,setOwn]=useState<ShinobiCharacter[]>([]);const [pub,setPub]=useState<ShinobiCharacter[]>([]);const [rivals,setRivals]=useState<ShinobiRival[]>([]);const [characterId,setCharacterId]=useState(activeCharacterId||'');const [rivalId,setRivalId]=useState('');const [note,setNote]=useState('');const [error,setError]=useState('');const refresh=async()=>{if(!user)return;const [o,p,r]=await Promise.all([listCharacters(user.id),listPublicCharacters(60),listRivals(user.id)]);setOwn(o);setPub(p);setRivals(r);if(!characterId&&o[0])setCharacterId(o[0].id)};useEffect(()=>{void refresh()},[user?.id]);const available=useMemo(()=>pub.filter(c=>c.id!==characterId&&!rivals.some(r=>r.character_id===characterId&&r.rival_character_id===c.id)),[pub,rivals,characterId]);if(!user)return <div className="screen"><h2>Sign in to track rivals</h2><button className="btn primary" onClick={()=>nav('/login')}>Sign In</button></div>;return <div className="screen social-page page-enter"><div className="dash-head"><div><span className="eyebrow">V10 · PHASE 3</span><h2>Rival Registry</h2></div><p>Pair one of your shinobi with a public rival, then carry that rivalry into matchup analysis.</p></div>{error&&<div className="generator-error">{error}</div>}<div className="social-tabs"><button onClick={()=>nav('/teams')}>Teams</button><button className="active">Rivals</button><button onClick={()=>nav('/matchups')}>Matchups</button></div><section className="box"><div className="section-title"><div><span className="eyebrow">DECLARE RIVAL</span><h3>Create a Rivalry</h3></div></div><div className="rival-builder"><select value={characterId} onChange={e=>setCharacterId(e.target.value)}>{own.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><span>VS</span><select value={rivalId} onChange={e=>setRivalId(e.target.value)}><option value="">Choose public rival…</option>{available.map(c=><option key={c.id} value={c.id}>{c.name} · {c.village||'Unknown Village'}</option>)}</select><input value={note} maxLength={200} onChange={e=>setNote(e.target.value)} placeholder="Why are they rivals? (optional)"/><button className="btn primary" disabled={!characterId||!rivalId} onClick={async()=>{try{await addRival(user.id,characterId,rivalId,note);setRivalId('');setNote('');await refresh()}catch(e){setError(e instanceof Error?e.message:'Could not add rival')}}}>Declare Rival</button></div></section><section className="box"><div className="section-title"><div><span className="eyebrow">RIVALRIES</span><h3>{rivals.length} Active</h3></div></div><div className="rival-grid">{rivals.map(r=><article className="rival-card" key={r.id}><div className="rival-side"><strong>{r.character?.name||'Your Shinobi'}</strong><small>{r.character?.clan||'Unknown Clan'}</small></div><b>VS</b><div className="rival-side"><strong>{r.rival?.name||'Public Rival'}</strong><small>{r.rival?.clan||'Unknown Clan'}</small></div>{r.note&&<p>{r.note}</p>}<div className="public-actions"><button className="btn secondary" onClick={()=>nav(`/matchups?left=${r.character_id}&right=${r.rival_character_id}`)}>Analyze Matchup</button><button className="btn ghost danger" onClick={async()=>{await removeRival(user.id,r.id);await refresh()}}>Remove</button></div></article>)}{!rivals.length&&<p className="muted">No rivalries declared yet.</p>}</div></section></div>}
