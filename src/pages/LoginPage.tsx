import {useState,type FormEvent} from 'react';import {Link,useLocation,useNavigate} from 'react-router-dom';import {useAuth} from '../contexts/AuthContext';
export function LoginPage(){const {signIn,configured}=useAuth();const nav=useNavigate();const location=useLocation();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');const message=await signIn(email,password);setBusy(false);if(message)setError(message);else nav((location.state as {from?:string}|null)?.from||'/account');};return <div className="screen auth-page page-enter"><div className="auth-card"><span className="eyebrow">CLOUD ARCHIVE</span><h2>Sign in</h2><p>Access your saved shinobi identities on any device.</p>{!configured&&<div className="generator-warning">Supabase is not configured yet. Add VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY to enable accounts.</div>}<form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={6}/></label>{error&&<div className="generator-error">{error}</div>}<button className="btn primary full" disabled={busy||!configured}>{busy?'Signing in…':'Sign in'}</button></form><p className="auth-switch">New here? <Link to="/signup">Create an account</Link></p></div></div>}
